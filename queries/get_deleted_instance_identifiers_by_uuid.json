{
    "displayName": "Deleted instance identifiers by UUID",
    "description": "Looks up deleted (audit) instance records by instance UUID and returns their HRID, title and identifiers, along with the date they were deleted. Enter one or more UUIDs separated by commas.",
    "instructions": "Enter one instance UUID, or multiple UUIDs separated by commas.",
    "parameters": [
        {
            "name": "uuids",
            "displayName": "Instance UUID(s)",
            "type": "text",
            "required": true
        }
    ]
}
