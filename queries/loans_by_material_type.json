{
    "displayName": "Loans by Material Type",
    "description": "Counts loans checked out during a specified date range, grouped by item material type.",
    "instructions": "Select a date range. Rows are limited to 1000 and may not download successfully at that size.",
    "parameters": [
        {
            "name": "start_date",
            "displayName": "Start date",
            "type": "date",
            "required": true
        },
        {
            "name": "end_date",
            "displayName": "End date (exclusive)",
            "type": "date",
            "required": true
        }
    ]
}
