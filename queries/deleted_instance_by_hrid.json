{
  "displayName": "Deleted Instance Lookup by HRID",
  "description": "Retrieve deleted inventory instance information from audit_instance__ using an HRID.",
  "query": "SELECT * FROM local.deleted_instance_by_hrid(:hrid);",
  "parameters": [
    {
      "name": "hrid",
      "displayName": "HRID",
      "description": "Enter an Instance HRID (for example: mig00005921542)",
      "required": true
    }
  ]
}
