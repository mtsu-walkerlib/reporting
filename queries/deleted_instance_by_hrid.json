{
  "displayName": "Deleted Instance Lookup by HRID",
  "description": "Find deleted inventory instances in the audit table by HRID.",
  "query": "SELECT * FROM local.deleted_instance_by_hrid(:hrid);",
  "parameters": [
    {
      "name": "hrid",
      "displayName": "Instance HRID",
      "description": "Enter the HRID to search for",
      "required": true
    }
  ]
}
