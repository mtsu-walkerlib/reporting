{
  "displayName": "Deleted Instance Lookup by HRID",
  "description": "Find deleted inventory instances from audit_instance__.",
  "query": "SELECT * FROM folio_inventory.deleted_instance_by_hrid(:hrid);",
  "parameters": [
    {
      "name": "hrid",
      "displayName": "HRID",
      "description": "Instance HRID (example: mig00005921542)",
      "required": true
    }
  ]
}
